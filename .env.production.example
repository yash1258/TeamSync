NEXT_PUBLIC_CONVEX_URL=
NEXT_PUBLIC_CONVEX_SITE_URL=
CONVEX_DEPLOYMENT=
# These are required at image build time and runtime.

# Planning Hub rollout flags.
# Set both values to true to expose /planning in UI and middleware.
NEXT_PUBLIC_FEATURE_PLANNING_HUB=true
FEATURE_PLANNING_HUB=true

# Optional: override port mapping used by docker-compose.
# APP_PORT=3010
